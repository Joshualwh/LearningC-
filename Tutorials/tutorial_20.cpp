#include <iostream>

using std::cout;

int main()
{
  bool found = false; // you can put 1 or any numbers other than 0, to represent true.
  //search algorithm -> if found, found = true;
  cout << std:: boolalpha << found << std::endl;
  if(found) cout << "This is true";

}